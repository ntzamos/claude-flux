{
  "name": "claude-flux",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/relay.ts",
    "dev": "bun run --watch src/relay.ts",
    "scheduler": "bun run src/scheduler.ts"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "grammy": "^1.21.1",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
